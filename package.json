{
  "name": "clouderp-platform",
  "version": "1.0.0",
  "description": "CloudERP Fullstack MERN Application",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "PATH=~/.nvm/versions/node/v20.20.2/bin:$PATH concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
